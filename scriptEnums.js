#pragma strict
enum playerState {
 	ready, waiting, disconnected
 	
 };
 
 enum rarity {
 	common , uncommon ,rare , doubleRare, tripleRare
 };
 
 enum cardTypes{
	hero, unit, plan, event
 };
 
 enum sets { 
  	first , second , third
 };
 
 enum areaOfEffect {
 	self, adj , all , odd ,even  
 };
 
 enum side { 
 	self, opposite
 };
 
 enum abilityEffects {
 
 };
 
 enum abilityTypes {
 
 };